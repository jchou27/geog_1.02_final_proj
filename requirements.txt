# GEOG 1.02 Final Project — Python dependencies
# Install with:  pip install -r requirements.txt

pandas>=1.5
numpy>=1.23
requests>=2.28
xarray>=2022.11
netCDF4>=1.6          # backend for xarray / GridSat / OISST NetCDF files
matplotlib>=3.6
cartopy>=0.21
Pillow>=9.0           # PIL — for GIBS image handling
scipy>=1.9            # smoothing in fig5
pydap>=3.3            # OPeNDAP backend for xarray / GridSat THREDDS access
